This program has a list of 10 elements. The code will remove
3 nodes, the first, middle, and last node. The output will
have the original list before the nodes are removed then
the second list will have the nodes after the 3 nodes are
removed.

To compile:
runMe
g++ -O2 -o runMe Linked_List.cpp